<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>form전송 결과</title>
    <!-- title: "GET",
    //form으로 받은 정보쿼리를 result.ejs에 객체로 정보보낸다
    userInfo: req.query, //{id, email, pw} -->
  </head>
  <body>
    <!-- 어떤 type의 요청인지 변수로 받음 -->
    <h1><%=title %>요청 확인하기</h1>
    <div>
      <span><%=userInfo.id %>(<%= userInfo.email %>)</span>님 환영합니다
    </div>
    <div>비밀번호는 <span><%=userInfo.pw %></span>이군요</div>

    <a href="/">홈으로 이동하기</a>
    <!-- ejs주석처리 앞에 #찍으면된다-->
    <%# ㅁㅁㅁㅁㅁㅁ %>
  </body>
</html>
